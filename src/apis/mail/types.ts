export type insertMailCodeOfRegisterResponse = {
  no: number;
  isSuccess: boolean;
  error: number;
};

export type expireMailCodeResponse = {
  isSuccess: boolean;
  error: number;
};

export type isExistMailCodeResponse = {
  isSuccess: boolean;
  error: number;
};
