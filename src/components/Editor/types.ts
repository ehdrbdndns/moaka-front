export type FallCardProps = {
  url: string;
};
export type Tag = {
  id: number;
  text: string;
};
export type BookMarkEditorProps = {
  profile: string;
  url: string | undefined;
};

export type EditorAProps = {
  profile: string;
  url: string;
  title: string;
  description: string;
};
